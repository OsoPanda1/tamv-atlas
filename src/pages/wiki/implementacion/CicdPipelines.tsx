import { WikiH1, WikiH2, WikiP, WikiBreadcrumb, WikiCard } from "@/components/WikiComponents";

export default function CicdPipelines() {
  return (
    <div>
      <WikiBreadcrumb section="implementacion" page="cicd-pipelines" />
      <WikiH1>CI/CD y Pipelines de Datos</WikiH1>
      <WikiP>El protocolo MD-X5 (Deca-V) ejecuta 10 ciclos de validación (lint, typecheck, test, build) antes de cada deploy. El ESLint plugin TAMV enforza leyes constitucionales (L1-L9) que protegen la integridad arquitectónica. El script check-architecture.ts construye grafos de dependencia para detectar violaciones.</WikiP>
    </div>
  );
}
